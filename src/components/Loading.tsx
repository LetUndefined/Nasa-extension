import { OrbitProgress } from "react-loading-indicators";

const Loading = () => {
  return (
    <div className="flex justify-center items-center">
      <OrbitProgress color="#22abde" size="medium" text="" textColor="" />;
    </div>
  );
};

export default Loading;
